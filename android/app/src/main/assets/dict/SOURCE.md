Bundled copy of `userscript/dict/en-zh-common.half10k.json` for Android MVP offline lookup.

- Source: skywind3000/ECDICT (MIT) — see `userscript/dict/SOURCE.md`
- Dict `version` is independent of app `versionName`
- Refresh path (download full dict) can replace this asset later
